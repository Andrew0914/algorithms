Add a new entry to MISTAKES_LOG.md based on the problem I just worked on.

Ask me the following questions one at a time, waiting for my answer before asking the next:

1. What problem were you working on?
2. What pattern does it use? (e.g., two pointers, BFS, hash map, etc.)
3. What difficulty? (Easy / Medium / Hard)
4. What went wrong? Where did you get stuck?
5. Why do you think it happened?
6. What should you have done instead?
7. Any tags? Suggest from: #misread-problem, #wrong-pattern, #edge-case, #off-by-one, #overcomplicated, #froze, #time-complexity, #syntax — or I can add custom ones.

After collecting all answers, append a new entry to MISTAKES_LOG.md under the `## Log` section using today's date, following the template format already in the file. Put the most recent entry at the top (right after the `<!-- Paste new entries below -->` comment).
