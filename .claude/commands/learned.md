Read MISTAKES_LOG.md and show me a numbered list of all entries that are NOT yet marked as learned (i.e., they don't have a **Status: Learned** line).

Display each entry as a one-liner: number, date, problem name, and tags.

Then ask me: "Which ones have you learned? (enter the numbers, e.g., 1, 3, 5)"

For each entry I select, ask me briefly: "What made it click for you?" (one question for all selected entries, they can answer in a short list).

Then update those entries in MISTAKES_LOG.md by adding at the end of each selected entry:

```
**Status:** Learned (YYYY-MM-DD)
**What made it click:** (their answer)
```

After updating, show a short summary: how many total entries, how many learned, how many still open.
